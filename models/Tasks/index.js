const { TaskModel, TaskView } = require('./TaskModel');
const { TaskFlowModel, TaskFlowView } = require('./TaskFlowModel');
const { TaskRuleModel, TaskRuleView } = require('./TaskRuleModel');
const { TaskPlanningModel, TaskPlanningView } = require('./TaskPlanningModel');

module.exports = {
  TaskModel,
  TaskView,
  TaskFlowModel,
  TaskFlowView,
  TaskRuleModel,
  TaskRuleView,
  TaskPlanningModel,
  TaskPlanningView,
};
